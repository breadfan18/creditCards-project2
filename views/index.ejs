<%- include('./partials/header') %>

<main>
    <h1>Credit Card Tracker App</h1>
    <% if(user) { %>
    <a href="/cards">Go to App</a>
    <% } else { %>
    <a href="/auth/google">Login With Google</a>
    <% } %>
</main>

<%- include('./partials/footer') %>